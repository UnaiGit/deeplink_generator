export interface StaffMember {
  name: string;
  tags: string[];
  active: boolean;
  rank: number;
  selected?: boolean;
  avatarUrl?: string;
  phone?: string;
  email?: string;
  hoursPerDay?: string;
  currency?: string;
  salaryPerHour?: string;
}


