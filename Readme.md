# Install all dependencies for both apps

npm install

# Run main_app only

npm run start:main

# Run panel_constructor only

npm run start:panel

# Run both apps at the same time

npm run start:both
